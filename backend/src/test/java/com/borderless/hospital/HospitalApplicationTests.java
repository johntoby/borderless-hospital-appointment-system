package com.borderless.hospital;

import org.junit.jupiter.api.Test;

/**
 * Basic test class for the Hospital Application.
 *
 * NOTE for students:
 *   A full @SpringBootTest would start the entire application context
 *   and require a running PostgreSQL database. For CI/CD pipelines,
 *   you would configure a test database (H2 in-memory, or Testcontainers).
 *
 *   For this teaching project, we keep tests minimal so the build
 *   succeeds with -DskipTests and doesn't require an external DB.
 */
class HospitalApplicationTests {

    @Test
    void contextLoads() {
        // This test verifies the test framework itself is configured correctly.
        // Expand this with unit tests for Services and integration tests for Controllers.
    }
}
